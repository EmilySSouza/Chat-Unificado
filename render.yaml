services:
  - type: web
    name: chat-unificado
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: TWITCH_CHANNEL
        value: funilzinha